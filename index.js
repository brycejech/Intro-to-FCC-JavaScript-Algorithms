'use strict';

const repeat        = require('./exercises/basic/repeat-a-string'),
      reverse       = require('./exercises/basic/reverse-a-string'),
      factorialize  = require('./exercises/basic/factorialize'),
      confirmEnding = require('./exercises/basic/confirm-ending'),
      booWho        = require('./exercises/basic/boo-who');

console.log(repeat);
console.log(reverse);
console.log(factorialize);
console.log(confirmEnding);
console.log(booWho);